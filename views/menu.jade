nav(class="navbar navbar-inverse navbar-fixed-top")
      div(class="container")
        div(class="navbar-header")
          button(type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar")
            span(class="sr-only") Toggle navigation
            span(class="icon-bar")
            span(class="icon-bar")
            span(class="icon-bar")
          a(class="navbar-brand" href="#") EdPurp
        div(id="navbar" class="collapse navbar-collapse")
          ul(class="nav navbar-nav")
            li(class="active")
              a(id="home" href="/home") Home
            li
              a(id="upload" href="/upload") Upload
            if user.admin
             li
              a(id="admin" href="/signup") Admin
          ul(class="nav navbar-nav navbar-right")
            li
              form(class="navbar-form navbar-right" action="/search" method="POST")
               input(type="text" name="search" class="form-control" placeholder="Search...")
            li(class="dropdown")
              a(id="user_dropdown" href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false") #{user.username} 
               span(class="glyphicon glyphicon-user")
              ul(class="dropdown-menu")
               li
                a(id="profile" href="/profile") Profile 
                 span(class="glyphicon glyphicon-cog")
               li
                a(href='/signout') Sign Out   
                 span(class="glyphicon glyphicon-off")
        ///.nav-collapse   
